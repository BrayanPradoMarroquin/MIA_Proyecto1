#include <iostream>
#include <string.h>
#include <stdio.h>
#include <fstream>

using namespace std;

void Analizador(string path){
    cout<<path<<endl;
}

int main(int argc, char *argv[]){
    Analizador(argv[3]);
    return 0;
}